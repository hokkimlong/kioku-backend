import { ActivityMemberGuard } from './activity-member-guard.guard';

describe('ActivityMemberGuardGuard', () => {
  it('should be defined', () => {
    expect(new ActivityMemberGuard()).toBeDefined();
  });
});
